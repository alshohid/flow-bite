import { NextResponse } from "next/server";

export function middleware(req: Request, res: Response) {
  // if (req.nextUrl.pathname.startsWith("/api/product")) {
  //   console.log(" i am inside product api ");
  //   return NextResponse.next()
  // }else if(req.nextUrl.pathname.startsWith('/api/profile')){
  //   console.log("this is inside  profile api ")
  //   return NextResponse.next()
  // }
}
